This sample shows the LwGrid component with the caption element:
  1. The first grid component uses text to render caption labels elements.
  2. The second grid component uses custom render to draw capltion labels elements.
